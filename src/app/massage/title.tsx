'use client';
import React, { useEffect, useRef } from 'react';
import useOnScreen from './useOnScreen';

export const Title = () => {
    return (
        <div>
            <h1>Message</h1>;
        </div>
    );
};
